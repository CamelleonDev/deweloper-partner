[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
# Deploy settings are per-service in Railway UI — do NOT put [deploy] here;
# it locks the same start/pre-deploy/healthcheck on app, worker, and cron.
